/**
 * setup.js
 * Vitest global setup file for React (jsdom) tests.
 * Imports @testing-library/jest-dom to add custom matchers
 * like toBeInTheDocument(), toHaveTextContent(), etc.
 */
import "@testing-library/jest-dom";
